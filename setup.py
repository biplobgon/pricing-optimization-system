"""Package setup placeholder for editable installs."""

from setuptools import find_packages, setup


setup(
    name="pricing-optimization-system",
    version="0.1.0",
    packages=find_packages(include=["src", "src.*"]),
)
